export const width = 400;
export const height = 400;

export const k = 30;
export const r = 10;
export const w = r / Math.sqrt(2);

export const cols = Math.floor(width / w);
export const rows = Math.floor(height / w);
